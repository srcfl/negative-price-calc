# OpenRouter API key (required for AI features)
OPENAI_API_KEY=sk-or-v1-your_openrouter_key_here

# Optional: Override default models
# Parser model (for file structure detection): default x-ai/grok-code-fast-1
# OPENAI_PARSER_MODEL=x-ai/grok-code-fast-1

# Explainer model (for Swedish analysis): default x-ai/grok-4.1-fast
# OPENAI_EXPLAINER_MODEL=x-ai/grok-4.1-fast

# Optional: Override base URL (default: https://openrouter.ai/api/v1)
# OPENAI_BASE_URL=https://openrouter.ai/api/v1

# Optional: Database configuration
DATABASE_PATH=data/price_data.db

# Optional: ENTSO-E API key for historical price data
# Get your free key at: https://transparency.entsoe.eu/usrm/user/createPublicApiKey
# ENTSOE_API_KEY=your_entsoe_api_key_here

# Note: Electricity price data is fetched from Sourceful API (no API key required)
# ENTSO-E is used as fallback for historical data when Sourceful lacks coverage
